import styled from "styled-components";

const List = styled.ul`
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    margin-left: -1rem;
    li {
        margin-left: 1rem;
    }
`

export default List;