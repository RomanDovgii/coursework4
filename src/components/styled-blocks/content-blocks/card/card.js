import styled from "styled-components";

const Card = styled.li`
    max-width: 10rem;
    display: flex;
    flex-direction: column;
    background-color: #fff;
    padding: 1rem 2rem;

`

export default Card;